#sidebar TOC

= search =

List of results for a query.

== Parameters: ==

|| *Name*  || *Type* || *Required* || *Description* ||
|| q       || string || required   || Search query string. ||
|| window  || string || optional   || Time window for results. (default: `auto`) Options: `auto` - automatically pick the most relevant window. `h` last hour, `d` last day, `w` last week, `m` last month, `a` all time ||

Accepts [ResListParameters List Parameters]

== Example: ==

{{{
http://otter.topsy.com/search.json?q=New+York
}}}
This will fetch search results in json format for the query new york.